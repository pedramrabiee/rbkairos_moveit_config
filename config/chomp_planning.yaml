# CHOMP Planning configuration for RB-Kairos+
# CHOMP is an optimization-based planner that produces smooth trajectories
# Better path quality than RRTConnect but slower

planning_plugin: chomp_interface/CHOMPPlanner
request_adapters: >-
  default_planner_request_adapters/AddTimeOptimalParameterization
  default_planner_request_adapters/FixWorkspaceBounds
  default_planner_request_adapters/FixStartStateBounds
  default_planner_request_adapters/FixStartStateCollision
  default_planner_request_adapters/FixStartStatePathConstraints
start_state_max_bounds_error: 0.1

# Planning parameters
planning_time_limit: 10.0
max_iterations: 200
max_iterations_after_collision_free: 5

# Cost weights
smoothness_cost_weight: 0.1
obstacle_cost_weight: 1.0
learning_rate: 0.01

# Smoothness costs
smoothness_cost_velocity: 0.0
smoothness_cost_acceleration: 1.0
smoothness_cost_jerk: 0.0

# Visualization (set false for faster planning)
animate_path: false
animate_endeffector: false
animate_endeffector_segment: "rbkairos_gripper_robotiq_85_base_link"

# Algorithm settings
add_randomness: false
hmc_discretization: 0.01
hmc_stochasticity: 0.01
hmc_annealing_factor: 0.99
use_hamiltonian_monte_carlo: false
ridge_factor: 0.0
use_pseudo_inverse: false
pseudo_inverse_ridge_factor: 1e-4

# Joint and collision settings
joint_update_limit: 0.1
collision_clearance: 0.2
collision_threshold: 0.07
random_jump_amount: 1.0

# Recovery
use_stochastic_descent: true
enable_failure_recovery: false
max_recovery_attempts: 5

# Trajectory initialization
trajectory_initialization_method: "quintic-spline"
